# Streamlit 主题配置（深色 / Codex 风格）
# Streamlit 1.61+ 不再支持 set_page_config(theme=)，主题须在此处设置。
# 注意：本文件仅控制 UI 外观，不写入任何密钥。
[theme]
base = "dark"
primaryColor = "#ff6b6b"
backgroundColor = "#0d1117"
secondaryBackgroundColor = "#161b22"
textColor = "#e6edf3"
font = "sans serif"
